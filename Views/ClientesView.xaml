<UserControl x:Class="SiTech.AgroLogistica.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <Grid Background="{StaticResource BrushCanvas}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="24,18,24,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="👥 Gestión de Clientes, Productores y Acopiadores" 
                           FontSize="20" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" />
                <TextBlock Text="Padrón comercial de empresas agropecuarias, cooperativas y molinos que operan con SiTech" 
                           FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,3,0,0" />
            </StackPanel>

            <Button Grid.Column="1" 
                    Content="+ Registrar Nuevo Cliente" 
                    Style="{StaticResource BtnPrimary}" 
                    Command="{Binding AbrirNuevoCommand}" />
        </Grid>

        <!-- Formulario Nuevo Cliente -->
        <Border Grid.Row="1" 
                Style="{StaticResource CardPanel}" 
                Margin="24,0,24,14" 
                Background="#F0FDF4" 
                BorderBrush="{StaticResource BrushPrimaryLight}"
                Visibility="{Binding MostrarDialogoNuevo, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Alta de Cliente / Cuenta Agropecuaria" FontWeight="Bold" FontSize="15" Foreground="{StaticResource BrushPrimaryDark}" Margin="0,0,0,10" />
                <UniformGrid Columns="3" Margin="0,0,0,8">
                    <StackPanel Margin="6">
                        <TextBlock Text="Razón Social *" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding NuevaRazonSocial, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="6">
                        <TextBlock Text="CUIT" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding NuevoCuit, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="6">
                        <TextBlock Text="Tipo de Entidad" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding NuevoTipo, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </UniformGrid>
                <UniformGrid Columns="4" Margin="0,0,0,8">
                    <StackPanel Margin="6">
                        <TextBlock Text="Localidad" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding NuevaLocalidad, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="6">
                        <TextBlock Text="Provincia" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding NuevaProvincia, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="6">
                        <TextBlock Text="Contacto Responsable" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding NuevoContacto, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="6">
                        <TextBlock Text="Teléfono" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding NuevoTelefono, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </UniformGrid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,6,0">
                    <Button Content="Cancelar" Style="{StaticResource BtnSecondary}" Margin="0,0,10,0" Command="{Binding CancelarNuevoCommand}" />
                    <Button Content="Guardar Cliente" Style="{StaticResource BtnPrimary}" Command="{Binding GuardarNuevoCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Contenido: Lista y Detalle -->
        <Grid Grid.Row="2" Margin="24,0,24,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.1*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1.2*" />
            </Grid.ColumnDefinitions>

            <!-- DataGrid -->
            <Border Grid.Column="0" Style="{StaticResource CardPanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" Height="36" />
                    </Grid>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Clientes}" SelectedItem="{Binding ClienteSeleccionado}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Razón Social" Binding="{Binding RazonSocial}" Width="*" FontWeight="SemiBold" />
                            <DataGridTextColumn Header="CUIT" Binding="{Binding Cuit}" Width="120" />
                            <DataGridTextColumn Header="Tipo" Binding="{Binding TipoCliente}" Width="130" />
                            <DataGridTextColumn Header="Localidad" Binding="{Binding Localidad}" Width="110" />
                            <DataGridTextColumn Header="Provincia" Binding="{Binding Provincia}" Width="90" />
                            <DataGridTextColumn Header="Hectáreas" Binding="{Binding HectareasOperadas, StringFormat='{}{0:N0} Ha'}" Width="85" />
                            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="80" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Ficha del Cliente -->
            <Border Grid.Column="2" Style="{StaticResource CardPanel}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel DataContext="{Binding ClienteSeleccionado}">
                        <TextBlock Text="Perfil de la Cuenta Comercial" FontSize="15" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,12" />

                        <Border Background="{StaticResource BrushPrimarySubtle}" CornerRadius="8" Padding="12" Margin="0,0,0,14">
                            <StackPanel>
                                <TextBlock Text="{Binding RazonSocial}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushPrimaryDark}" />
                                <TextBlock Text="{Binding Cuit, StringFormat='CUIT: {0}'}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource BrushPrimaryMedium}" />
                                <TextBlock Text="{Binding TipoCliente}" FontSize="11" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0" />
                            </StackPanel>
                        </Border>

                        <TextBlock Text="DATOS DE CONTACTO Y UBICACIÓN" FontSize="11" FontWeight="Bold" Foreground="{StaticResource BrushTextLight}" Margin="0,4,0,8" />

                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Localidad y Provincia:" FontSize="12" Foreground="{StaticResource BrushTextMuted}" />
                            <TextBlock Text="{Binding Localidad, StringFormat='{}{0}, '}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right" />
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Contacto Directo:" FontSize="12" Foreground="{StaticResource BrushTextMuted}" />
                            <TextBlock Text="{Binding ContactoNombre}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right" />
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Teléfono Corporativo:" FontSize="12" Foreground="{StaticResource BrushTextMuted}" />
                            <TextBlock Text="{Binding Telefono}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right" />
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Correo Electrónico:" FontSize="12" Foreground="{StaticResource BrushTextMuted}" />
                            <TextBlock Text="{Binding Email}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right" />
                        </Grid>

                        <TextBlock Text="VOLUMEN DE OPERACIÓN" FontSize="11" FontWeight="Bold" Foreground="{StaticResource BrushTextLight}" Margin="0,10,0,8" />

                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Hectáreas Vinculadas:" FontSize="12" Foreground="{StaticResource BrushTextMuted}" />
                            <TextBlock Text="{Binding HectareasOperadas, StringFormat='{}{0:N0} Ha'}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource BrushPrimary}" HorizontalAlignment="Right" />
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Contratos Vigentes:" FontSize="12" Foreground="{StaticResource BrushTextMuted}" />
                            <TextBlock Text="{Binding ContratosVigentes, StringFormat='{}{0} Activos'}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource BrushBlue}" HorizontalAlignment="Right" />
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
